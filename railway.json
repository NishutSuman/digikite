{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd digikite-server && npm install --include=dev && npm run db:generate"
  },
  "deploy": {
    "startCommand": "cd digikite-server && npm run db:deploy && npm start",
    "healthcheckPath": "/api/v1/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
